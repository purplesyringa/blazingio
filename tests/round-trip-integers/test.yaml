type: round-trip
config:
  architecture: x86_64,aarch64
  os: linux,windows
  # Make sure both LUTful and LUTless implementations are correct
  lut: [y, n]
  # Make sure overloads don't resolve to floats
  float: [y, n]
  interactive: [y, n]
