type: round-trip
config:
  architecture: x86_64,aarch64
  os: windows,linux,macos
  # Make sure overloads don't resolve to floats
  float: [y, n]
  # Make sure char is not resovled to (un)signed char
  char_with_sign_is_glyph: [y, n]
  interactive: [y, n]
